// const filterCourseCategories = (courseData, event) => {
//     if (event.target.value === "") {
//       //  setBrand(event.target.value);
//       setCourseData(coursesData);
//     } else {
//       //  setBrand(event.target.value);
//       setCourseData(
//         coursesData.filter(
//           (course) => course.category.indexOf(event.target.value) >= 0
//         )
//       );
//     }
//   };

//   const filterCourseLevels = (courseData, event) => {
//     if (event.target.value === "") {
//       setCourseData(coursesData);
//     } else {
//       setCourseData(
//         coursesData.filter(
//           (course) => course.level.indexOf(event.target.value) >= 0
//         )
//       );
//     }
//   };



//   export  {filterCourseCategories, filterCourseLevels};








